const styles = (theme) => ({
  background: {
    // backgroundColor: theme.color.background,
    padding: 40,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    minHeight: '100vh',
    textAlign: 'center',
    flex: '1 0 auto',
  },
  textField: {
    color: '#fff',
  },
  backgroundImage: {
    position: 'absolute',
    width: '100%',
  },
});
export default styles;
