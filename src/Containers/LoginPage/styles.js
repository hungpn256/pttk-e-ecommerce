const styles = (theme) => ({
  background: {
    background: '#fff',
    position: 'relative',
    padding: 40,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    minHeight: '100vh',
    textAlign: 'center',
    flex: '1 0 auto',
    overflow: 'hidden',
  },
  textField: {},
  backgroundImage: {
    position: 'absolute',
    width: '110%',
    minWidth: 1200,
    overflow: 'hidden',
  },
  login: {
    width: 400,
    background: 'rgba(255,255,255,0.7)',
    zIndex: 1,
  },
});
export default styles;
